'use strict';

/** Default subscription_status for new SaaS tenant rows (never subscribed). */
const SAAS_INITIAL_SUBSCRIPTION_STATUS = 'expired';

module.exports = {
  SAAS_INITIAL_SUBSCRIPTION_STATUS
};
